# graphql-fullstack-dabbling

GraphQl Integration using Apollo, lowdb, and React.


### Start it:

* Start server using  `yarn server` 
* Start client using  `yarn app` 
* Access site on `localhost:1234`
